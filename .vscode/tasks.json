{
    "tasks": [
        {
            "type": "shell",
            "label": "cl.exe build active file",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/Fe:",
                "${fileDirname}\\execu\\${fileBasenameNoExtension}.exe",
                // "/Fo:",
                // "${workspaceFolder}\\obj\\*.obj",
                "${workspaceFolder}\\src\\*.cpp",
                "${file}"
            ]
        }
    ],
    "version": "2.0.0"
}